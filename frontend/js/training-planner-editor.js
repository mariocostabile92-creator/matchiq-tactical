(function(){const T=window.MatchIQTraining;T.readEditor=()=>{const plan=structuredClone(T.state.plan.current_plan);document.querySelectorAll(".session").forEach(card=>{const session=plan.sessions[Number(card.dataset.index)];card.querySelectorAll("[data-field]").forEach(input=>session[input.dataset.field]=input.type==="number"?Number(input.value):input.value);card.querySelectorAll(".drill").forEach(box=>{const drill=session.drills[Number(box.dataset.drill)];box.querySelectorAll("[data-drill-field]").forEach(input=>drill[input.dataset.drillField]=input.type==="number"?Number(input.value):input.value)})});return plan}})();
