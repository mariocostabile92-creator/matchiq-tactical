(function(){const D=window.MatchIQDecision;D.renderSources=()=>{const options=D.state.current?.options||[],seen=new Map();options.flatMap(item=>item.sources||[]).forEach(source=>seen.set(`${source.source_type}:${source.source_id}`,source));const items=[...seen.values()];D.els.sourcesSection.hidden=!D.state.current;D.els.sources.innerHTML=items.length?items.map(item=>`<article class="source-item"><a href="${D.escape(item.action_url||'/knowledge.html')}">${D.escape(item.title)}</a><p>${D.escape(item.summary)}</p><span class="chip">${D.escape(item.reliability_level)}</span></article>`).join(""):'<div class="empty">Nessuna fonte verificabile collegata. Le opzioni restano limitate.</div>'};})();
